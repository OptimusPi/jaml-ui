# Committed defaults for the demo. Override locally via demo/.env.local
# (which is gitignored). VITE_CDN_BASE_URL is where motely-wasm is hosted —
# the demo composes the runtime URL as ${VITE_CDN_BASE_URL}/motely-wasm/<version>/index.mjs.
VITE_CDN_BASE_URL=https://cdn.seedfinder.app
